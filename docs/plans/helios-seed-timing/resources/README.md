# Shared Resources

- **[commands.md](commands.md)** — every local, login-node, monitoring, verification and
  retrieval command, plus the result directory layout and array-index mapping.
- **[metrics-schema.md](metrics-schema.md)** — the `cf_metrics_*.csv` schema: what each timing
  column means per method, the CCHVAE timing gap Stage 1 closes, the quality metric list, the
  `target_class` asymmetry, and the aggregation conventions Stage 5 must follow.

Referenced from stages by relative path, e.g. `see [commands](../resources/commands.md)`.
