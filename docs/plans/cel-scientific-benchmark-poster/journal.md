# Journal

Append-only. Newest entries at the bottom. Never rewrite an earlier entry.

One entry per invocation, in this shape:

```
## YYYY-MM-DD HH:MM -- Stage N: [Name] -- DONE
**Did**: [1-3 lines]
**Verification**: GATE lines passed. REPORT values: [metric]=[value]
**Provenance**: [per measured GATE: the input the value was read from, and the defect that would
turn it red] - [or `NOT MEASURED` for any that could not be produced from this run's own inputs]
**Problems**: [symptom -> root cause -> resolution -> inline/subagent] or "none"
**Commit**: `abc1234`
```

---

## 2026-08-29 13:12 -- Stage 1: Freeze Poster Claims and Identity -- DONE
**Did**: Added source-derived identity and method-name registries plus a TeX table extractor that generated 22 stable claims (17 publishable, 4 qualified, 1 contradiction resolved by omission). Added an independent validator for exact identity, headings, scope consistency, comparative verdicts, result cells, and byte-for-byte regeneration.
**Verification**: GATE `node poster/research/scripts/validate-claims.mjs` passed; placeholder scan returned no matches; 16 selected results resolved to named manuscript table cells. Independent mutation audit passed after proving affiliation, venue, naming, heading, peer comparison, sparsity, count-drift, prose-value, and literal-value defects turn red. REPORT claim counts: publishable=17, qualified=4, excluded=0, contradictory=1.
**Provenance**: Scope values are parsed from the current manuscript declarations and cross-checked with conclusion/table totals; result values are independently parsed from the named TeX cells and compared exactly with the generated ledger; identity and aliases resolve to the camera-ready block, repository metadata, README, and manuscript sources. Any changed declaration, row, metric heading/cell, identity field, comparison peer, or unresolved contradiction turns validation red.
**Problems**: The initial conclusion anchor mismatch was repaired by a focused worker. Two independent audits exposed false-pass paths (unchecked identity/headings/verdicts, then decorative row anchors and hard-coded count text); repairs added live-source and independent cell checks. `uv run pytest` cannot collect because `GPyOpt` is absent in the existing project environment; deferred as B-1 because it is unrelated to this Node-only poster stage.
**Commit**: `this commit` (`docs(poster): freeze CEL claims and identity`)

## 2026-08-29 13:23 -- Stage 2: Collect NeurIPS Poster References -- DONE
**Did**: Added five ranked official NeurIPS source records, a reproducible downloader, a generated byte manifest, ignored local research PNGs, and source-cited visual inspection notes. Strengthened verification to bind each live event page to its exact live asset and current local bytes.
**Verification**: GATE `node poster/research/neurips/verify.mjs` passed for five unique official PNGs; every source has an official virtual page plus media asset, and all 35 direct observations cite a valid source ID. REPORT sizes/dimensions: OpenXAI 4,553,335 B (4320×2880); M4 2,191,086 B (3900×2600); tabular benchmark 2,199,651 B (3744×2807); ADBench 967,150 B (4320×2880); human-centered XAI 3,711,307 B (4817×2870). Redirects=0; failing sources=0.
**Provenance**: The verifier reads `sources.json`, current live page HTML and asset responses, current local bytes, the generated manifest, and `notes.md`; it checks page-to-asset binding, media/final URL, remote/local byte equality, PNG chunk order and CRC through terminal IEND, dimensions, size, SHA-256, uniqueness, coverage, and observation IDs. Substitution, corruption, and swapped page/asset mutations independently turned red.
**Problems**: The initial verifier trusted URL-shaped literals and PNG headers, so a regenerated manifest could certify substituted bytes; a focused worker added live binding and full structural validation, and an independent mutation audit passed. The repository Python suite remains unavailable under B-1 and was not rerun.
**Commit**: `this commit` (`docs(poster): catalog NeurIPS benchmark poster references`)

## 2026-08-29 13:57 -- Stage 3: Synthesize Guidelines and Storyboard -- DONE
**Did**: Added 18 preserve/adapt/avoid decisions, an exact A0 visual specification, a claim-mapped left-to-right storyboard, a closed poster-content contract, and a validator that binds visible assertions to the claim ledger and source files.
**Verification**: GATE `node poster/research/scripts/validate-brief.mjs` passed; exact A0/canvas/safe-area/print/font/type-conversion checks passed; reading order plus substantive thirty-second/two-minute narratives passed; independent mutation audit passed for source, claim, numeric, QR, geometry, narrative, and result-table bypasses. REPORT section heading+copy word count=179/180; result visuals=4; expected self-contained bundle asset budget=3,000,000 bytes.
**Provenance**: The validator reads the current NeurIPS source/observation files, planning findings, claim ledger, identity links, manuscript source files, visual spec, storyboard, and content contract. The headline argument is composed from current claim verdicts; all substantive storyboard assertions carry live claim/source mappings; local-reference facts are checked against the linked planning evidence. Unknown sources, empty/mismatched claim mappings, unsupported prose, copied numerals, duplicate QR ownership, unsafe geometry, or table dumps turn red.
**Problems**: Initial copy exceeded budget by four words and was tightened by a focused worker. Two audit rounds exposed permissive source filtering, under-mapped qualifiers, free prose, numeric-prefix, local-reference, and HTML-table bypasses; the final closed-schema validator passed all reproduced mutations. Browser inspection of the local `file://` bundle was blocked by browser policy, so no workaround was attempted; tracked planning findings supplied the local-reference facts. Python suite remains deferred under B-1.
**Commit**: `this commit` (`docs(poster): define CEL poster guidelines and storyboard`)

## 2026-08-29 14:17 -- Stage 4: Scaffold the A0 Web Artifact -- DONE
**Did**: Initialized and recovered the generated React/TypeScript/Vite/Tailwind foundation, added a typed adapter over all frozen poster inputs, and implemented semantic poster primitives, a fixed native canvas, asymmetric layout, print-only scaling, and screen controls. Added contract tests that bind structure, dimensions, safe area, colors, print scale, and content resolution to the source JSON.
**Verification**: GATE `pnpm --dir poster/cel-benchmark-poster run typecheck` passed; GATE `pnpm --dir poster/cel-benchmark-poster test` passed 7 tests; GATE `pnpm --dir poster/cel-benchmark-poster run build` passed; claim and brief validators passed. Independent verification confirmed the semantic regions, frozen-input linkage, and physical output of 1189 × 840.887 mm inside the 1189 × 841 mm A0 page. REPORT production sizes: HTML=502 bytes, CSS=48,562 bytes, JS=218,166 bytes, HTML+CSS+JS=267,230 bytes (below the 3,000,000-byte budget).
**Provenance**: The adapter reads the current generated claims, content contract, identity, method names, and visual specification; layout tests read the same visual specification and compare live CSS tokens plus the CSS-pixel-to-millimetre scale. Missing claims, placeholders, changed canvas/safe-area/color/grid values, incorrect print geometry, absent semantic regions, or visible toolbar output at print turn the relevant gate red.
**Problems**: The required initializer hit a macOS `sed` incompatibility after creating the project; a focused recovery preserved and completed the generated foundation. pnpm initially blocked the esbuild lifecycle script, resolved with a narrow workspace allow-list. Independent audit caught a dimensionless print scale that would have occupied only about one quarter of A0 width and several unbound layout literals; all were repaired by deriving dimensions and labels from frozen inputs and adding mutation-sensitive tests. Repository Python tests remain deferred under B-1.
**Commit**: `this commit` (`feat(poster): scaffold print-first CEL artifact`)

## 2026-08-29 14:36 -- Stage 5: Implement Evidence Graphics and Poster Content -- DONE
**Did**: Extended the source-derived ledger with the manuscript's two-backbone scope claim, then completed the poster with a real repository QR, five-stage CEL protocol SVG, claim-formatted scope facts, four focused evidence findings, explicit rounded-zero/missing/conditional semantics, selection guidance, precedent citations, and reproducibility links. Added rendered-claim, numeric-ownership, source-citation, pipeline, QR, and link audits.
**Verification**: GATE claim and brief validators passed with 23 claims (18 publishable, 4 qualified, 1 contradictory), 18 source-backed design rules, 10 sections, 4 result visuals, and 179/180 section heading+copy words; GATE Vitest passed 10/10; GATE typecheck and production build passed. Browser inspection reported all 10 sections contained, zero section/canvas overflow, 42 rendered claim markers, 4 findings, one labelled repository QR, and no console diagnostics. REPORT per-section words: header=15, problem=15, scope=13, protocol=15, local=15, group=19, regression=18, applicability=18, guidance=35, reproducibility=16. REPORT production sizes: HTML=502 bytes, CSS=52,065 bytes, JS=247,749 bytes, HTML+CSS+JS=300,316 bytes; full dist=314,869 bytes.
**Provenance**: Scope values and result displays resolve through regenerated claim IDs; the numeric DOM audit requires every visible evidence number to sit inside a live claim-owned element. Links and QR resolve through identity JSON, source notes resolve through poster-content citations, precedents resolve to live Related Works citations, and the protocol taxonomy carries the method-scope claim. Changed table cells, manuscript scope, unknown claim/source IDs, unowned result numerals, bad links, duplicate/misdirected QR, missing pipeline stages, or altered taxonomy ownership turn a gate red.
**Problems**: The Stage 5 brief required two backbones but Stage 1 had not frozen that count, so the extractor and independent validator were extended and the generated ledger grew from 22 to 23 claims. Browser inspection exposed min-content overlap between the main and lower bands; the group finding moved into the lower evidence band, its comparison became a compact paired matrix, and grid-item sizing plus SVG heights were corrected until every section fit. Independent verification then found `Reports` embedded inside `Metrics` and taxonomy provenance attached to `Data`; a distinct Reports node and targeted ownership test resolved the blocker. Python suite remains deferred under B-1.
**Commit**: `this commit` (`feat(poster): add CEL benchmark story and evidence`)

## 2026-08-29 14:48 -- Stage 6: Audit Facts, Print Layout, and Readability -- DONE
**Did**: Added a pinned Playwright render harness, current native-canvas PNG and one-page A0 PDF exports, DOM claim/layout audits, and Poppler PDF verification with a print-derived review PNG. Tightened essential chart labels to 12 px and preserved the five-stage pipeline inside its section.
**Verification**: GATE claim and brief validators passed with 23 claims, 42 visible claim markers, and 17 source notes; Vitest passed 10/10 and TypeScript passed. GATE render/layout audit passed for 10 contained sections on the exact 1800 × 1273 canvas with no document overflow, safe-area breach, missing asset, or print toolbar. GATE Poppler audit passed for one `3370.08 × 2383.92 pt` A0 landscape page. Independent cold fact audit mapped every visible benchmark value and scope fact to its claim/manuscript source and passed identity, names, links, QR, qualifiers, and limitations. Independent PDF-derived visual review passed hierarchy, order, legibility, contrast, chart semantics, whitespace, alignment, clipping, and QR presentation. REPORT minimum text sizes: body=15 px, caption=9 px, chart label=12 px. REPORT artifact sizes: preview PNG=372,271 bytes, PDF=415,903 bytes, print-review PNG=758,217 bytes. REPORT residuals: provenance/footer source text is supplemental and near the lower comfortable size; repeated `BOTTOM` labels remain spatially unambiguous.
**Provenance**: The render harness serves the current production `dist`, records failed requests, waits for fonts, and exports the DOM at native scale. Layout measurements come from the live current render and compare each named section/figure against the frozen safe bounds; zero or missing font measurements fail. The claim audit regenerates the ledger and maps current DOM markers, source notes, scope facts, links, QR, and identity. `pdfinfo` and `pdftoppm` read this run's PDF; the independently audited PDF SHA-256 is `074c4afe23b9cefc8f9bb83b9be8945b8e307c4e774bc46fa4ff5b1473858ef5`. Wrong dimensions/page count, a changed source value, missing qualifier, unowned marker, clipping, overflow, bad link/QR, or failed request turns the relevant gate red.
**Problems**: The first safe-bottom calculation double-counted the canvas top border and produced a false breach; the audit now derives the inner bottom from the outer rectangle and border width. Measured chart labels were initially 9 px, so essential labels were raised to 12 px and the pipeline compressed to retain containment. The first cold fact audit found that TeX `--` had been described as an em dash; the wording was corrected to `missing-value marker`, the ledger was regenerated and the entire artifact rerendered, and the fresh cold audit passed. The repository Python suite remains deferred under B-1.
**Commit**: `this commit` (`test(poster): verify CEL print artifact and claims`)

## 2026-08-29 14:56 -- Stage 7: Bundle and Package Deliverables -- DONE
**Did**: Used the named web-artifact bundler to produce a self-contained `bundle.html`, extended the render harness with a network-blocked bundle mode, and added offline, PDF-content, package, checksum, and PDF-derived-preview verification. Packaged the offline HTML, verified A0 PDF, PDF-derived PNG, checksums, and a revisioned handoff README.
**Verification**: GATE `pnpm run verify:all` passed the current 23-claim ledger, 18-rule brief, TypeScript, 10/10 tests, Vite build, Parcel single-file build, offline render, native layout, DOM claim audit, PDF audit, packaging, and checksum audit. GATE offline render passed with 10 sections, 42 claim markers, zero required remote requests, and no console/load failures. GATE `shasum -a 256 -c SHA256SUMS` passed all three final artifacts. PDF remained one `3370.08 × 2383.92 pt` A0 landscape page and Poppler text inspection found all 10 required section contents. The independent package verifier passed byte identity, offline behavior, checksums, PDF-derived preview identity, README completeness, visual containment, and intended-file status. REPORT final sizes: HTML=303,052 bytes, PDF=415,903 bytes, PDF-derived PNG=758,217 bytes, README=1,662 bytes, checksums=285 bytes. REPORT residuals remain supplemental provenance/footer text near the lower comfortable size and repeated but spatially clear `BOTTOM` labels.
**Provenance**: `bundle.html` is produced from the current source by the named Parcel/html-inline skill script; the offline audit serves only that file, blocks non-local requests, and checks the live DOM. The packaged HTML hash `a89de23e1b96610ce0ddf382c2161f55a2fc415acd810a83193d0291104bee31` matches the root bundle exactly. Poppler regenerates the final preview from the run's PDF and the package audit compares exact hashes; the final PDF hash is `ba86310191a99ff3c3820c4e153e7727b7f8c323da5412cf3668d71d9c3c108c` and the preview hash is `3dbb8281bf86153994b2c220e41dc83de1df0cfbbe0f9fd13130adfdd2c28e5d`. A required remote asset, incomplete section/claim DOM, wrong page geometry, missing PDF section text, stale preview, altered artifact, or incomplete README turns the relevant gate red.
**Problems**: The named bundler's first install stopped because pnpm rejected four native build scripts; the project allow-list was narrowed to `@parcel/watcher`, `@swc/core`, `lmdb`, and `msgpackr-extract`, after which the same bundler completed. Its generated `.parcel-cache` was added to the project ignore list while `.parcelrc`, bundle inputs, package scripts, and deliverables remain tracked. One manual checksum recheck was initially invoked from the repository root, where manifest-relative filenames cannot resolve; the documented deliverables-directory invocation passed all three files. The repository Python suite remains deferred under B-1.
**Commit**: `this commit` (`feat(poster): package CEL scientific poster deliverables`)

## 2026-08-30 00:34 -- Post-plan revision: Benchmark-first manuscript graphics -- DONE
**Did**: Reframed the poster around CEL as a controlled benchmark for counterfactual explanations, foregrounded the manuscript motivation and three stated contributions, and replaced the derived evidence graphics with five focused views of manuscript figures covering the architecture and local, global, group-wise, and regression results. Extended the source-derived claim ledger and regenerated the offline HTML, one-page A0 PDF, and PDF-derived preview.
**Verification**: GATE `pnpm --dir poster/cel-benchmark-poster run verify:all` passed the 30-claim ledger, 18-rule brief, TypeScript, 10/10 tests, Vite build, self-contained Parcel bundle, network-blocked offline audit, layout and claim audits, A0 PDF audit, packaging, and package audit. GATE `shasum -a 256 -c SHA256SUMS` passed all three deliverables. REPORT section heading+copy words=175/180; manuscript figures=5; required remote requests=0; PDF pages=1; PDF size=3370.08 × 2383.92 pt; minimum body/caption/chart text=15/9/12 px.
**Provenance**: Motivation, contribution, and result-summary claims are anchored to the current manuscript introduction, benchmark, results, and conclusion text; the displayed graphics are deterministic derivatives of the tracked manuscript figure files. The browser audits inspect the current render and embedded bundle, while Poppler verifies and rerenders this run's PDF. A missing source anchor, changed figure path, unowned claim, unresolved asset request, layout breach, wrong page geometry, stale preview, or altered packaged byte turns a gate red.
**Problems**: Parcel emitted manuscript images as root-relative URLs, so basename-only data inlining produced invalid `/data:image/...` requests in the standalone artifact; the bundling wrapper now replaces complete relative/root-relative path tokens, rejects leftover emitted references, and the offline audit passes with zero remote requests. The manuscript's sparsity-direction inconsistency remains explicitly qualified, so the poster preserves the source figure but makes no comparative sparsity ranking.
**Commit**: `this commit` (`feat(poster): frame CEL as a CE benchmark`)

## 2026-08-30 10:18 -- Post-plan revision: Shorter copy and exact manuscript title -- DONE
**Did**: Replaced the poster and document title with the manuscript title verbatim. Removed repeated identity and layout labels, shortened contributions and result takeaways, retained the source figures and essential sparsity caveat, and enlarged the architecture/regression panels. Expanded the global/group-wise crops to retain their method labels.
**Verification**: GATE `pnpm run verify:all` passed: 30 source-backed claims, 11/11 tests, TypeScript, both builds, offline loading, layout, exact title, PDF, and package audits. GATE manual SHA-256 recheck passed all three deliverables. REPORT section heading+copy words=88 (previously 175); visible HTML poster words=223 (previously 375, excluding unchanged text embedded in graphics); manuscript figures=5; required remote requests=0; PDF pages=1 at A0 landscape. Final PDF-derived PNG was visually inspected with no clipping.
**Provenance**: The title is checked against identity JSON, itself validated against the current manuscript; browser text measurements enforce the concise copy budget. The current render, PDF, and packaged bytes supply the layout and checksum evidence.
**Problems**: Enlarging the global/group-wise crops initially exceeded their row heights by five pixels; adjusted the right-column row allocation and reran the full pipeline successfully.
**Commit**: `this commit` (`feat(poster): shorten copy and match manuscript title`)

## 2026-08-30 12:54 -- Post-plan revision: CE example, revised columns, and reference logos -- DONE
**Did**: Replaced the top-left motivation with a concise CE definition and labelled before/after toy loan example; moved global results below it. Preserved the center architecture/local-result panels at their prior measured dimensions and positions. Moved contributions to top right and group-wise results below, removed the regression panel, and copied the PWr, genwro.AI, and Tooploox logos unchanged from the supplied PUMAL reference poster into the header.
**Verification**: GATE `pnpm run verify:all` passed 31 source-backed claims, 15/15 tests, exact manuscript title, nine sections, three result visuals, column order/alignment, logo hashes and non-overlap, the explicitly illustrative example, offline loading, one-page A0 PDF, and package checksums. Manual SHA-256 recheck passed all deliverables. REPORT heading+copy words=80/110; visible text words=232/260; manuscript graphics=4; reference logos=3; required remote requests=0. Final PDF-derived preview visually inspected with no clipping.
**Provenance**: The new definition is anchored to the manuscript introduction. Invented example inputs and the approval threshold are isolated in `ce-example.json`, labelled as toy data on the poster, and tested to flip the declared prediction without claiming experimental provenance. `brand-assets.json` records the reference path and SHA-256 for each byte-identical source logo. Live browser rectangles confirm both center panels retain their previous positions and sizes.
**Problems**: Initial example spacing exceeded its panel by four pixels; reduced only its internal gaps/margin. PDF text extraction uppercased the styled illustrative label; its content check now compares case-insensitively while the manuscript-title check remains exact. Both issues were resolved before the successful full verification.
**Commit**: `this commit` (`feat(poster): add CE example and refine benchmark layout`)

## 2026-08-30 12:58 -- Post-plan revision: Project QR inside Extend -- DONE
**Did**: Moved the unique repository QR from the header into the Extend contribution, beside the open-source library description. Removed the reserved header QR column and retained the exact title, logos, and evidence-panel layout.
**Verification**: `pnpm run verify:all` passed all 15 tests, source/brief checks, offline build, layout, claims, one-page A0 PDF, and package audits. Tests and the live claim audit now require exactly one QR owned by `contribution.library` in the right-column contributions section. Manual checksums passed; the final PDF-derived preview was visually inspected without clipping.
**Provenance**: QR payload and label still resolve to the frozen project identity. The vector QR has a four-module quiet zone and is contained within the Extend card.
**Problems**: none.
**Commit**: `this commit` (`feat(poster): move project QR to Extend contribution`)

## 2026-08-30 13:09 -- Post-plan revision: Multi-feature CE example and clean annotations -- DONE
**Did**: Replaced the dark single-feature example with a light, side-by-side six-feature applicant profile. Income and monthly debt change; age, employment, credit history, and loan amount remain fixed. Highlighted only the changed counterfactual values and retained the prediction flip. Removed printed source labels, model disclaimers/rules, and marginal provenance notes; kept dataset captions and all provenance in project files/non-printing metadata.
**Verification**: `pnpm run verify:all` passed 31 source-backed claims, 15/15 tests, exact manuscript title, the two-of-six feature-change audit, fixed-feature preservation, prediction consistency, light-background and hidden-metadata checks, offline loading, one-page A0 PDF, and package audits. Manual checksums passed. REPORT visible words=194/260; body/caption/chart text=15/12/12 px. Final PDF-derived preview visually inspected without clipping.
**Provenance**: `ce-example.json` records the invented profiles and affordability rule separately from manuscript evidence. Tests confirm both changed features are actionable, the other four are unchanged, and neither single change alone flips the stated example prediction. Source mappings and the existing sparsity caveat remain available in metadata; no sparsity ranking was added.
**Problems**: Initial table spacing clipped the change-count summary; reduced cell padding without shrinking text. The first PDF annotation scan also matched the word “source” inside “open-source”; narrowed it to source-label punctuation and reran the complete pipeline successfully.
**Commit**: `this commit` (`feat(poster): refine multi-feature counterfactual example`)

## 2026-08-30 13:19 -- Post-plan revision: Centered title and split header logos -- DONE
**Did**: Centered the exact manuscript title and author block between equal-width logo columns. Placed PWr on the left and genwro.AI above Tooploox on the right. Removed the requested subtitle from the visible poster and content contract; preserved all evidence panels and the contribution QR.
**Verification**: `pnpm run verify:all` passed 31 source-backed claims, 15/15 tests, exact manuscript title, centered-title geometry, logo ordering and non-overlap, offline loading, one-page A0 PDF, and package audits. Manual SHA-256 checks passed all three deliverables; `git diff --check` passed. REPORT heading+copy words=73/110; visible words=187/260; required remote requests=0. Final PDF-derived preview visually inspected without clipping.
**Provenance**: Logo assets remain byte-identical to the supplied reference poster. Browser geometry checks verify the title midpoint matches the header midpoint and the right-side logos share a center line. PDF text extraction checks the removed subtitle is absent; package verification binds the preview to the current PDF.
**Problems**: none.
**Commit**: `this commit` (`feat(poster): center title and arrange header logos`)

## 2026-08-30 13:22 -- Post-plan revision: Remove top line and venue label -- DONE
**Did**: Removed the teal line at the top of the canvas and the printed XKDD 2026 label. Preserved the centered manuscript title and split logo arrangement; retained the venue in research metadata only.
**Verification**: `pnpm run verify:all` passed all 15 tests, source/brief checks, offline build, layout, claims, one-page A0 PDF, and package audits. New checks require a zero-width canvas top border and no printed venue marker in the header or PDF. Manual checksums and final PDF-derived visual inspection passed; visible words=185/260.
**Problems**: none.
**Commit**: `this commit` (`feat(poster): remove top line and venue label`)

## 2026-08-30 13:57 -- Post-plan revision: Conference logos left, institutional logos right -- DONE
**Did**: Added the existing XKDD PNG and ECML-PKDD SVG assets to the left header stack. Moved PWr above genwro.AI and Tooploox on the right, preserving a centered manuscript title and each asset's proportions. Added conference asset hashes and side ownership to the brand manifest; extended the self-contained bundler to embed SVG assets.
**Verification**: `pnpm run verify:all` passed all 15 tests, source/brief checks, five-logo inventory and byte hashes, left/right stack ordering and non-overlap, offline rendering, one-page A0 PDF, and package audits. Manual SHA-256 recheck and final PDF-derived visual inspection passed. REPORT embedded graphics=9; logos=5; required remote requests=0; visible words=185/260.
**Provenance**: Conference files already existed as untracked project assets at the start of this request and were used unchanged. Their external origins were not recorded and are not asserted. The original three institutional logos remain unchanged copies from the supplied reference poster.
**Problems**: none.
**Commit**: `this commit` (`feat(poster): add conference logos and regroup header branding`)

## 2026-08-30 14:01 -- Post-plan revision: Enlarge and simplify architecture section -- DONE
**Did**: Removed the Shared evaluation protocol caption, tinted panel background, and outline. Cropped only outer whitespace through a CSS viewport at source coordinates 90/28/1220/622, leaving the 1400 × 735 manuscript raster unchanged. Expanded the schema to the full 828-pixel center-column width and blended its white image background into the poster paper.
**Verification**: `pnpm run verify:all` passed 16/16 tests, source/brief checks, source-crop geometry and enlarged viewport checks, transparent panel checks, caption absence in the PDF, offline rendering, one-page A0 export, and packaging. Manual SHA-256 recheck and `git diff --check` passed. Final PDF-derived preview visually inspected: all schema boxes and arrows remain visible, with no panel background, repeated caption, or overlap. REPORT visible words=182/260.
**Provenance**: The architecture asset and source mapping remain unchanged; cropping is presentation-only. All manuscript diagram content is preserved.
**Problems**: none.
**Commit**: `this commit` (`feat(poster): enlarge schema and simplify architecture panel`)

## 2026-08-30 14:12 -- Post-plan revision: Framework scope grid and unified right-column results -- DONE
**Did**: Moved benchmark scope below the center-column framework and restructured it into a two-row, three-column facts grid. Added the source-derived classification metric count. Unified global, local, and group-wise manuscript results under one Results section above contributions on the right; retained concise finding headings and dataset captions while removing repeated summary paragraphs. Rebalanced the body columns to give the results more width and compacted the contribution rows without shrinking the QR.
**Verification**: `pnpm run verify:all` passed 32 source-backed claims, 16/16 tests, scope-grid geometry, framework/scope order, unified result ownership/order, source and identity checks, offline loading, one-page A0 PDF, and package audits. Manual SHA-256 recheck and final PDF-derived visual inspection passed. REPORT section heading+copy words=47/110; visible words=159/260; manuscript graphics=4; required remote requests=0.
**Provenance**: The metric total is counted from parsed classification table columns, checked against the other classification schemas and supplementary inventory, and independently verified from the live categorical-table header. The nine metrics include runtime and count proximity once; the visible label specifies classification metrics rather than the full library registry. All manuscript graphic files remain unchanged.
**Problems**: The first render clipped the last result caption by several pixels because the right-column content exceeded its available height. Reduced contribution padding and corrected the QR column width; the full verification and final visual review then passed.
**Commit**: `this commit` (`feat(poster): unify results and restructure benchmark scope`)

## 2026-08-30 14:20 -- Post-plan revision: Remove result captions and correct local plot scale -- DONE
**Did**: Removed all three printed Adult Census / method-paradigm captions while retaining dataset identity in accessible metadata. Reflowed the local manuscript's five metric panels into a larger, centered three-plus-two arrangement, using non-destructive source-coordinate crops with uniform image scaling. Preserved the global and group-wise figure proportions, all axes, and all method labels. Compacted the contribution cards and QR within the existing right-column section to accommodate the enlarged local plots.
**Verification**: `pnpm run verify:all` passed 17/17 tests, source and brief checks, five-metric crop inventory, natural image aspect-ratio checks, two-row placement, caption absence in the PDF, offline rendering, one-page A0 export, and package checks. Manual SHA-256 recheck passed. Final PDF-derived preview visually inspected with complete plot labels and no clipping. REPORT visible words=144/260; local metric panels exceed 175 pixels wide and 100 pixels high. Raster label review is now explicitly distinguished from measured DOM text in the layout audit.
**Provenance**: Manuscript image files remain unchanged. The local crop rectangles cover the complete Adult Census row and reflow its metric panels without altering values, axes, or scales within any plot.
**Problems**: The first reflow clipped several pixels at the bottom of the Results section. Reduced the section gap, aligned the compact contribution headings, and reran the full pipeline successfully.
**Commit**: `this commit` (`feat(poster): remove result captions and enlarge local plots`)

## 2026-08-30 14:30 -- Post-plan revision: Named benchmark inventory tiles -- DONE
**Did**: Replaced the six numeric scope facts with four named inventory tiles: datasets, methods, backbones per task, and classification metrics. Grouped methods into Local, Global, and Group-wise; removed the standalone folds and paradigm-count tiles. Listed the full benchmark inventory while keeping regression result panels off the poster.
**Verification**: `pnpm run verify:all` passed 32 source-backed claims, 18/18 tests, four-tile geometry, exact inventory matching, PDF name coverage, removed-count checks, offline rendering, one-page A0 export, and package audits. Manual SHA-256 recheck and `git diff --check` passed. The final PDF-derived preview was visually inspected with complete schema content and no clipped tile text. REPORT visible words=247/260; inventory body text=15 px; required remote requests=0.
**Provenance**: Dataset names are parsed from the manuscript dataset table, with Give Me Some Credit expanded from the supplementary text. Method names are parsed from actual table rows (10 local, two global, two group-wise), rather than the stale local multirow count. Backbone names come from the manuscript model descriptions; metric names map the validated classification-table columns. Browser and PDF audits compare the printed inventories to these source-derived records. The previous PDF check banning CEARM and Wachter anywhere was narrowed to regression result sections because those method names are now explicitly requested inventory content.
**Problems**: The first render clipped six pixels at the architecture section's bottom after the scope expanded. Reduced only the center-column gap from 24 to 16 pixels; the full pipeline and final visual review then passed without shrinking the diagram or inventory text.
**Commit**: `this commit` (`feat(poster): name benchmark inventories in scope tiles`)

## 2026-08-30 14:35 -- Post-plan revision: Remove center headings and divider -- DONE
**Did**: Removed the printed One evaluation framework and Benchmark scope headings and the teal divider between their sections. Removed the divider's padding; retained the diagram scale, four named inventory tiles, and accessible section names.
**Verification**: `pnpm run verify:all` passed 19/19 tests, source and brief checks, DOM absence of center headings and a zero-width scope border, PDF text absence of both headings, offline rendering, one-page A0 export, and package audits. Manual checksums and final PDF-derived visual inspection passed without clipping. REPORT visible words=242/260; body text=15 px; required remote requests=0.
**Problems**: The storyboard validator initially treated the new layout-only instruction as a scientific claim; registered that exact structural line without changing scientific provenance checks. The new accessibility test initially queried prior test renders; scoped it to its own render container. Both were corrected before the successful complete verification.
**Commit**: `this commit` (`feat(poster): remove center headings and divider`)

## 2026-08-30 14:38 -- Post-plan revision: Schema-aligned scope tile styling -- DONE
**Did**: Restyled the four inventory tiles as rounded pale-blue containers with dashed dark-blue outlines and cream, solid-border heading boxes, following the manuscript diagram's module/node styling. Replaced teal count accents with dark-blue sans-serif counts. Preserved every inventory name, the two-by-two layout, and 15-pixel body text. Updated the visual brief to allow the requested schema-aligned rounded tiles.
**Verification**: `pnpm run verify:all` passed 19/19 tests, source and inventory checks, live rounded-border/palette checks, containment, offline rendering, one-page A0 export, and package audits. Manual checksums and `git diff --check` passed. Final PDF-derived preview visually inspected: all four tiles fit, their headings remain on one line, and the schema and result panels remain unchanged. REPORT visible words=242/260; body text=15 px; required remote requests=0.
**Provenance**: Styling follows visual inspection of the unchanged manuscript architecture raster; no benchmark content or source graphics were altered.
**Problems**: none.
**Commit**: `this commit` (`feat(poster): align scope tiles with manuscript schema`)

## 2026-08-30 14:43 -- Post-plan revision: Longer tile dashes and capitalized titles -- DONE
**Did**: Capitalized the four inventory titles using title case. Replaced browser-defined dashed borders with decorative SVG outlines using 10-pixel dashes, 5-pixel gaps, and a 2-pixel stroke, slightly thicker than the prior 1.5-pixel border. Preserved rounded corners, colors, inventory text, and spacing.
**Verification**: `pnpm run verify:all` passed 19/19 tests, exact heading and inventory checks, live SVG dash/stroke measurements and container-bound checks, offline rendering, one-page A0 export, and package audits. Manual checksums and `git diff --check` passed. Final PDF-derived preview visually inspected with longer dashes, capitalized headings, and no clipping. REPORT visible words=242/260; body text=15 px; required remote requests=0.
**Problems**: The storyboard's scientific-number validator treated new pixel dimensions as benchmark claims. Kept that check intact and described the styling qualitatively in the storyboard; precise dimensions remain in CSS and the live layout audit.
**Commit**: `this commit` (`feat(poster): refine tile dashes and capitalize headings`)

## 2026-08-30 14:46 -- Post-plan revision: Dataset inventory grouped by task -- DONE
**Did**: Split the existing 18 Datasets tile into Classification and Regression groups, preserving all names, the total count, and the rounded dashed styling. Task assignments are parsed from the manuscript table rather than inferred from dataset names.
**Verification**: `pnpm run verify:all` passed 20/20 tests, manuscript-derived task counts, exact grouped inventories, layout containment, offline rendering, one-page A0 export, and package audits. Manual checksums and `git diff --check` passed. Final PDF-derived preview visually inspected with both dataset groups readable and no clipping. REPORT visible words=243/260; body text=15 px; required remote requests=0.
**Provenance**: The Task column in `manuscript/main_lncs.tex#tab:datasets_complete` assigns 13 rows to classification and five to regression. Extraction checks those row counts against the manuscript's stated totals; the new test checks every named assignment.
**Problems**: none.
**Commit**: `this commit` (`feat(poster): group datasets by prediction task`)

## 2026-08-30 14:48 -- Post-plan revision: Remove horizontal section dividers -- DONE
**Did**: Removed the horizontal rules below the header, above the footer, and above the example, results, and contributions sections. Preserved table row rules, plot axes, schema boxes, dashed scope-tile outlines, and vertical contribution accents.
**Verification**: `pnpm run verify:all` passed 20/20 tests, source and content checks, zero measured top/bottom borders on all ten semantic sections, no horizontal-rule elements, offline rendering, one-page A0 export, and package audits. Manual checksums and `git diff --check` passed. Final PDF-derived preview visually inspected with no section dividers or clipped content. REPORT visible words=243/260; body text=15 px; required remote requests=0.
**Problems**: none.
**Commit**: `this commit` (`feat(poster): remove horizontal section dividers`)

## 2026-08-30 14:59 -- Post-plan revision: Center contributions and framed regression results -- DONE
**Did**: Moved Three contributions and its Extend QR beneath the four center-column inventory tiles. Compacted tile spacing and arranged contributions in a single row while preserving the full-width architecture and 15-pixel inventory text. Added the manuscript regression figure's complete Concrete row as a fourth right-column result category. Framed each category with a transparent, rounded SVG rectangle using the existing long-dash styling; no standalone section dividers were restored.
**Verification**: `pnpm run verify:all` passed 20/20 tests, source and content checks, new center-column ownership and ordering, four result frames with measured transparent fills and matching bounds, regression crop/aspect geometry, offline rendering, one-page A0 export, and package audits. Manual checksums and `git diff --check` passed. Final PDF-derived preview visually inspected: all schema boxes, tile inventories, contribution text, QR, and result axes remain visible. REPORT visible words=247/260; body text=15 px; manuscript graphics=5; required remote requests=0.
**Provenance**: Reused the existing regression raster without altering it. The presentation-only crop at 0/0/1100/225 retains the full Concrete row and all five metrics, including MAE. Its heading maps to the existing manuscript regression overview claim and does not assert universal dominance or add aggregate numerical claims. Updated the superseded no-regression and right-column-contribution contracts to match this explicit request.
**Problems**: The bundler initially retained the old four-graphic inventory count; updated it to require five manuscript graphics plus five logos. The first layout render clipped four pixels from the architecture section because the expanded center content exceeded its height. Reduced architecture top padding from ten to four pixels, preserving its scale; all subsequent checks passed.
**Commit**: `this commit` (`feat(poster): center contributions and frame regression results`)

## 2026-08-30 15:43 -- Post-plan revision: Practical local, global, and group-wise examples -- DONE
**Did**: Replaced the left-column six-feature profile with a three-feature local comparison and three vector plots. Local applicant A changes income and debt payments while full-time employment stays fixed. Global applies the same income increase to applicants A-D. Group-wise applies a shared debt-payment reduction to A-B and a shared income increase to C-D. All original points are declined; all nine arrows and their endpoints lie in the approved region after crossing the same decision boundary. Kept the center and right columns, manuscript graphics, title, logos, and QR unchanged. Updated the offline HTML, A0 PDF, and PDF-derived preview; no commit requested.
**Verification**: `pnpm run verify:all` passed all 23 tests, manuscript claim checks, action/profile consistency, fixed-feature invariants, shared plot scales, exact arrow direction and approval of arrow tips, label containment at twelve-pixel minimum, offline rendering, one-page A0 export, and package checks. Compared center/right column geometry against HEAD: unchanged. Manual SHA-256 checks and `git diff --check` passed. Inspected the complete PDF-derived preview and enlarged left-column crop; no clipping or overlapping labels. REPORT visible words=305/320; the budget increased only to accommodate the two additional requested practical examples.
**Provenance**: The user-approved invented applicant values and actions are recorded in `ce-example.json`, separately from benchmark evidence. The comparison follows the supplied PUMAL presentation. Retained the undisplayed age, credit-history, and loan-amount values. Adjusted the illustrative affordability threshold from 2800 to 2600 so all approved endpoints and arrowheads sit visibly beyond the boundary; no displayed applicant values or manuscript results changed. All originals remain declined. The global fixed action is a concrete example, not a restriction on every global CE method.
**Problems**: The first draft overflowed the left column. Reduced redundant copy and table/section spacing and used compact, identically scaled plots without reducing text sizes or changing the other columns. The final full verification and visual checks passed.
**Commit**: `this commit` (`feat(poster): illustrate local global and group-wise recourse`)

## 2026-08-30 15:53 -- Post-plan addition: Matplotlib example generator -- DONE
**Did**: Added `poster/plots/plot_ce_examples.py`, tests, usage documentation, and generated PNG/SVG figures for local, global, group-wise, and side-by-side examples. The generator reads the existing example JSON and directly reuses the observation, counterfactual, arrow, and decision-boundary helpers from `cel/plotting/plot_utils.py`. Preserved tab10 originals, orange counterfactuals, translucent black arrows, and the library contour style; enlarged arrowheads and used the light grid from `counterfactual_visualization.py`. Coordinates are normalized for the helper APIs and formatted as monthly euro values on the axes. The existing React poster and PDF remain unchanged.
**Verification**: All 15 focused pytest cases passed, including all nine declined-to-approved transitions, fixed features, shared actions and populations, classifier projection, arrow geometry, actual helper provenance, Matplotlib marker colors, equal axes, text containment, transparent exports, and CLI options. Ruff lint and format checks passed. Visually inspected the regenerated comparison and enlarged group-wise figure with readable captions and complete axes. `git diff --check` passed.
**Provenance**: Reused the existing illustrative data and model rule without changes. No synthetic distributions or benchmark measurements were added. Generated assets remain separate from the final poster deliverables until an integration request.
**Problems**: Importing CEL's top-level package requires the unavailable optional GPyOpt dependency. Loaded the actual plotting utility module directly by file path, avoiding unrelated eager model imports without modifying CEL. Its existing `torch.meshgrid` future-warning is documented and does not affect rendering.

## 2026-08-30 16:04 -- Post-plan revision: Plot-only Matplotlib assets integrated -- DONE
**Did**: Removed figure titles, action captions, and standalone legends from the Matplotlib generator. Cropped the figure canvas to the axes area with consistent margins and retained axis labels, applicant IDs, decision-region labels, points, and arrows. Replaced the poster's hand-authored SVG plots with the three generated transparent Matplotlib SVGs. Kept the poster's native paradigm labels, action text, and feature table; center/right columns and all manuscript graphics are unchanged. Outlined SVG fonts preserve exact type rendering in the browser and PDF.
**Verification**: `pnpm run verify:all` regenerated the figures and passed 15 Python plotting tests, 23 poster tests, source/claim checks, offline bundling with thirteen image assets, loaded-vector checks, equal aspect ratios, native-equivalent label size above twelve pixels, A0 export, and package checks. Inspected the full PDF-derived preview and enlarged left-column crop without clipping. Center/right geometry matches HEAD; manual checksums, Ruff checks, and `git diff --check` passed.
**Provenance**: Embedded non-printing SVG metadata ties each asset to hashes of the example JSON, CEL plotting utility, and generator. Checks compare generated transitions against the shared example data and require each rendered arrow layer. The build regenerates assets before testing and rejects stale sources. Existing illustrative values and prediction rule were not changed.
**Problems**: Parcel interpreted direct SVG module imports as objects in the offline bundle. Switched to the same static `new URL(..., import.meta.url)` asset pattern already used for poster logos. The complete pipeline and final visual checks then passed.

## 2026-08-30 16:10 -- Post-plan revision: Group-wise plot legend -- DONE
**Did**: Added a single-row legend beneath the group-wise plot with both group colors for Original points, an orange Counterfactual point, and the actual Decision boundary line style. Added the same legend beneath the group-wise panel in the comparison asset. Preserved the physical axes size, shared scales, transparent backgrounds, outlined fonts, and title-free figures. Regenerated the poster HTML, A0 PDF, and preview; no commit requested.
**Verification**: `pnpm run verify:all` passed 16 Python plotting tests, 23 poster tests, manuscript and source checks, offline rendering, layout checks, A0 export, and package checks. Tests verify legend labels, marker and boundary styles, placement below the x-axis label, and unchanged axes dimensions. PDF-skill visual review of the full preview and enlarged group-wise crop found no clipping. SHA-256 checks, Ruff lint and format checks, and `git diff --check` passed.
**Problems**: The added legend initially caused a 2.5-pixel overflow at the bottom of the left column. Reduced left-column example gaps and top spacing without shrinking plots or legend text. The final layout audit and visual review passed.

## 2026-08-30 16:19 -- Post-plan revision: Remove footer and tighten header spacing -- DONE
**Did**: Removed the bottom Reproduce and extend strip, installation command, and repository/documentation links. Retained the Code & project QR inside the Extend contribution. Removed the unused footer component and styles, moved hidden precedent citations into the canvas, and updated the content inventory and storyboard to ten sections. Reduced header-to-body spacing from 38px to 12px by removing the eighteen-pixel header bottom padding and reducing the grid gap. Preserved the manuscript title, logos, figure sizes, and all column content.
**Verification**: `pnpm run verify:all` passed 16 plotting tests and 24 poster tests, manuscript and brief validation, offline rendering, exact twelve-pixel header gap, absence of the footer, plot dimensions, one-page A0 export, PDF text checks, and package checksums. The PDF skill's full-page visual review confirmed clear header spacing, no footer, and an unclipped group-wise legend. Regenerated the HTML, PDF, and PDF-derived preview. No commit requested.
**Problems**: The brief validator interpreted a numeric spacing value as an unsupported scientific literal; used its established prose convention instead. Scoped the new QR-label assertion to its rendered poster to avoid matching earlier test renders. The final complete verification passed.

## 2026-08-30 16:44 -- Post-plan revision: Consistent typography and larger figure labels -- DONE
**Did**: Set the printed Georgia title to 96pt and unified example/result subheadings at 36pt. Made Arial explicit for body text and generated figures, normalized bold weights to 700, and kept the title centered with its original two-line grouping. Added poster-only typography derivatives for the five manuscript graphics. The architecture retains original vector boxes and connectors with larger outlined Arial labels and adjusted line breaks. Result figures retain lossless source PNG plot interiors and uniform scaling, with larger outlined labels, original dataset/method identities, and the local three-plus-two arrangement. Regression density ticks use k for thousands. Manuscript files and benchmark statistics are unchanged.
**Verification**: `pnpm run verify:all` passed 23 Python tests and 25 poster tests, manuscript/brief validation, offline rendering, provenance checks, A0 layout, PDF text, and package checksums. Tests compare every embedded result crop pixel against its source PNG and the schema artwork against the original PDF conversion. Typography audits check source hashes, actual SVG label bounds, schema-container containment, at least 22pt manuscript labels, the 96pt title, and 36pt subheadings. PDF text extraction independently confirmed a 96.00pt title and approximately 36pt subheadings. The PDF skill's full-page and enlarged visual reviews passed. Ruff lint/format, SHA-256 verification, and `git diff --check` passed. Updated HTML, PDF, and PDF-derived preview; no commit requested.
**Provenance**: Source crop rectangles, tick strings and gridline anchors, dataset identities, method order, minimum font sizes, source hashes, generator hashes, and embedded image hashes are recorded in the generated SVG metadata and reproducible generator. Original boxes, whiskers, medians, and grids are reused rather than reconstructed from statistics. Logos, example data, and the shared decision rule remain unchanged.
**Problems**: Larger labels initially overflowed the right column and several schema nodes. Adjusted line breaks and reserved label margins, then compacted figure whitespace without reducing the requested type sizes. Tick anchors snap to visible source gridlines, allowing for grid segments covered by filled boxes. Updated the stale footer reading-order contract and normalized generated SVG whitespace. Final audits and visual review passed.

## 2026-08-30 16:58 -- Post-plan revision: Workshop A1 portrait layout -- DONE
**Did**: Changed the print target to the user-supplied A1 portrait specification, 594 × 841 mm. Reflowed the concept examples and framework into asymmetric upper columns, with a full-width two-by-two results grid below. Kept all three examples, four scope tiles, contributions beneath the tiles, four result categories, original manuscript graphics, logos, and project QR. Preserved the exact manuscript title at 96pt; adapted subheadings to 28pt, body copy to approximately 18pt, and figure labels to at least 17pt. Increased Matplotlib example label sizes, kept 13.5 mm safe margins, and retained the compact header gap. Updated print controls, geometry tests, storyboard, output contracts, and packaging to the new A1 PDF filename. The previous A0 PDF remains unchanged as a historical reference, not the workshop print file.
**Verification**: `pnpm run verify:all` passed 23 Python tests, 25 poster tests, claim and brief validation, offline rendering, layout and label bounds, one-page A1 PDF size/content, and package checksums. Strengthened the layout audit to catch clipped intermediate grid containers and check the two-by-two results placement. PDF text extraction confirmed approximately 96pt title and 28pt subheadings. The PDF skill's full-page and enlarged render reviews found no clipping, including the complete schema, example legend, and bottom result frames. Ruff, manual SHA-256 checks, and `git diff --check` passed. Source manuscript and CEL library files are unchanged. No commit requested.
**Problems**: The first portrait layout compressed auto-sized rows and clipped the schema and example legend. Used max-content rows, rebalanced the native canvas/type scale, and tightened contribution QR spacing. Prevented sticky screen controls from covering tall portrait preview screenshots. Retained historical reference geometry explicitly in the design-brief validator while enforcing A1 for current output.

## 2026-08-30 17:07 -- Post-plan revision: Smaller title and bottom contributions -- DONE
**Did**: Reduced the Georgia title from 96pt to 80pt, preserving the exact manuscript wording and centered logo arrangement. Moved all three contributions and the Extend QR into a full-width closing band below every result panel, with the section heading beside the cards. Tightened vertical body gaps from 18px to 14px. Updated section ownership, reading order, typography contracts, tests, storyboard, and deliverables documentation. Preserved A1 portrait geometry, safe margins, all plot dimensions, and every manuscript/example asset.
**Verification**: `pnpm run verify:all` passed 23 Python tests and 26 poster tests, manuscript and brief validation, offline rendering, layout and label bounds, one-page A1 PDF size/content, and package checksums. Added a regression test for the final contribution/QR section and a geometric check that it spans the page below results. Independent PDF extraction measured the title at approximately 80pt. The PDF skill's full-page and enlarged bottom-band reviews found no clipping. Manual SHA-256 checks and `git diff --check` passed. Regenerated HTML, PDF, and PDF-derived preview. No commit requested.
**Problems**: A separate heading above the bottom cards exceeded the safe area. Changed the contributions to a compact horizontal band and slightly reduced inter-section gaps without shrinking figures or QR. Updated the existing provenance test that still expected the QR in the center column.

## 2026-08-30 17:17 -- Post-plan revision: Swap left header logos -- DONE
**Did**: Swapped the left-side logo order to ECML-PKDD above XKDD. Updated the brand manifest order, accessible group label, logo-order test, layout audit, and storyboard. Preserved original logo files, dimensions, right-side branding, and all poster content.
**Verification**: `pnpm run verify:all` passed 23 Python tests, 26 poster tests, manuscript/brief validation, offline rendering, logo hashes/order, A1 layout and PDF checks, and package checksums. The PDF skill's final visual review passed. Pixel comparison with the committed PDF-derived preview found changes only within the left-logo rectangle (42, 70)-(175, 230); every other preview pixel is unchanged. Refreshed HTML, PDF, and PNG exports. No commit requested.

## 2026-08-30 17:26 -- Post-plan revision: Restore original manuscript diagram -- DONE
**Did**: Restored the architecture diagram from `manuscript/figures/teaser.pdf` as an unmodified vector conversion of its existing CropBox. Preserved the original Poppins and Canva Sans glyphs, labels, line breaks, boxes, and connectors; removed poster-only Arial relabeling. Added complete source-vector equality and hash checks. Original schema labels now print at approximately 13.6pt, an explicit exception to the unchanged 17pt minimum for result labels. Preserved result artwork, poster layout, and the previous left-logo swap. Refreshed HTML, PDF, and PNG exports; no commit requested.
**Verification**: Passed 23 Python tests, 27 poster tests, type checking, build, manuscript/brief validation, offline rendering, A1 layout, PDF content, and package checksums. The initial verification pipeline passed its tests and build; after resolving the bundler issue below, all export and audit stages passed in a resumed sequence. The PDF skill's full-page and enlarged diagram reviews confirmed original glyphs and unclipped artwork. Ruff checks and manual SHA-256 verification passed. Source manuscript, CEL library, and example graphics are unchanged.
**Problems**: Parcel's SVG optimization stalled on the restored manuscript glyph definitions. Disabled optimization only for the architecture SVG, preserving its source vectors and normal optimization for other assets. Added a regression test for that scoped configuration; the subsequent bundle completed successfully.

## 2026-08-30 17:51 -- Post-plan revision: Apply PDF design review -- DONE
**Did**: Reflowed Global into three-plus-two source plots with wider axes and a shared numbered key retaining every original method name. Aligned result-frame bottoms within each row, added 12px frame padding, and strengthened Results to 32pt bold. Increased scope inventory font size, line spacing, group separation, and padding; prevented individual names from splitting and aligned the tiles with the example-column bottom. Changed approved example markers to diamonds, strengthened arrows without changing endpoints, and matched the boundary legend to its opaque teal contour. Replaced contribution action labels with the existing statements as bold top-aligned headings, keeping the QR in its own column. Preserved the exact 80pt title, logos, original manuscript diagram, section order, all source boxplot pixels, and example data.
**Verification**: `pnpm run verify:all` passed 25 Python tests and 28 poster tests, source claims and brief validation, type checking, both builds, offline rendering, A1 safe-area/layout checks, PDF content, and package checksums. Added checks for Global's method-key mapping and enlarged source axes, marker shapes and legend styling, equal result-frame heights, contribution alignment, scope alignment, and stronger Results typography. The PDF skill's final full-page and enlarged visual reviews passed. Ruff lint/format, manual SHA-256 verification, and `git diff --check` passed. Refreshed HTML, PDF, and PDF-derived preview. Source manuscript and CEL library files are unchanged. No commit requested.
**Problems**: Added padding initially pushed the bottom band approximately 9mm beyond the safe area. Tightened only small gaps and table padding in the example column, preserving its plot dimensions and font sizes. Prevented name-level wrapping after PDF extraction detected a split Log-density label. Updated the brief validator's explicit structural-line inventory for the new design instructions. The final complete verification passed.

## 2026-08-30 18:09 -- Post-plan revision: Unboxed contribution strip -- DONE
**Did**: Replaced the contribution cards with an unboxed closing strip. Moved the section heading above three equal-width messages, with small teal ordinals, 25pt bold navy headings, and 18pt supporting lines. Updated the manuscript-backed claim generator and ledger to the approved concise wording: Controlled protocol / Reproducible evaluation; Cross-paradigm benchmark / Local · Global · Group-wise; Extensible library / Open source. Removed card fills and colored bars. Kept the QR inside the library contribution but anchored it to the whole strip, preserving its size, quiet zone, destination, and label. The strip fits within the existing bottom area without moving other sections.
**Verification**: `pnpm run verify:all` passed 25 Python tests and 29 poster tests, manuscript claims and brief validation, type checking, both builds, offline rendering, native layout, PDF content, and package checksums. New audits verify exact contribution wording, equal-width unboxed messages, aligned headings/supporting lines, ordinal styling, QR placement, and printed type sizes. The PDF skill's full-page and enlarged contribution-strip reviews passed. Pixel comparison against the committed PDF-derived preview found changes only inside the contribution band, bounded by (38, 2218)-(1646, 2346); every pixel above it is unchanged. All non-contribution section rectangles are unchanged. Manual SHA-256 verification and `git diff --check` passed. Source manuscript, logos, CEL library, and all figure assets are unchanged. Refreshed HTML, A1 PDF, and preview. No commit requested.

## 2026-08-30 18:24 -- Post-plan revision: Contribution typography and GitHub QR -- DONE
**Did**: Renamed the section to Contributions and matched its typography exactly to Results: 32pt bold Georgia. Enlarged the three contribution headings to the result-title size, 28pt Georgia, with bold weight. Preserved the 18pt supporting lines and unboxed strip. Enlarged the QR from 60px to 96px (43.2mm at A1), embedded the official GitHub Invertocat with its original clear space, and removed all visible caption text while preserving its accessible repository link. Added asset provenance and hash checks, high error correction, and a four-module outer quiet zone. Refreshed the offline HTML, A1 PDF, PDF-derived preview, and documentation. No commit requested.
**Verification**: `pnpm run verify:all` passed 25 Python tests and 31 poster tests, source claims and brief validation, type checking, both builds, offline rendering with the embedded QR logo, layout, PDF content, and package checksums. Added actual PDF-pixel QR decoding at 72 and 150 DPI; both resolve to the existing repository URL with error correction H. The PDF skill's full-page and enlarged strip reviews confirmed clear typography, centered logo, no caption, and no clipping. All non-contribution section rectangles are unchanged from HEAD. Ruff lint/format and `git diff --check` passed. Manuscript sources, header logos, CEL library, and generated figure assets are unchanged.
**Problems**: The initial layout audit incorrectly required an inline image URL in the regular build. Kept the local-asset check for that build and moved the strict inline-logo requirement to the offline-bundle audit. All final checks passed.

## 2026-08-30 18:26 -- Post-plan revision: Rename metrics tile -- DONE
**Did**: Changed the visible tile label from Classification Metrics to Counterfactual Explanations Metrics, retaining the count of nine, its inventory, and existing typography. Updated the label assertions and added a PDF text check. Regenerated the offline HTML, A1 PDF, and PDF-derived preview. No commit requested.
**Verification**: All 31 poster tests, type checking, both builds, offline rendering, layout, manuscript claims and brief checks, PDF content, QR decoding at 72/150 DPI, and package checksums passed. The PDF skill's visual review confirmed that the longer label fits on one line without resizing or clipping. All section rectangles remain identical to HEAD; `git diff --check` passed.

## 2026-08-30 18:28 -- Post-plan revision: Rebalance right header logos -- DONE
**Did**: Increased the PWr logo dimensions by 15% and reduced genwro.AI and Tooploox by 10%, preserving aspect ratios, original image files, centered stacking, and spacing. Added size regression checks and regenerated the offline HTML, A1 PDF, and PDF-derived preview. No commit requested.
**Verification**: All 32 poster tests, type checking, both builds, offline rendering, layout, manuscript claims and brief checks, PDF content, QR decoding at 72/150 DPI, and package checksums passed. The PDF skill's visual review confirmed balanced logos with no clipping or overlap. Section rectangles, header dimensions, title and author text positions, and both left-side logos are unchanged from HEAD. Manuscript, CEL, generated figures, and original brand assets are unchanged; `git diff --check` passed.
