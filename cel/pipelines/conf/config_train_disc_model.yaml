defaults:
  - _self_
  - disc_model: mlr

hydra:
  sweeper:
    max_batch_size: 10

experiment:
  output_folder: models/

dataset:
  _target_: cel.datasets.DigitsDataset

disc_model:
  epochs: 500
  batch_size: 128
  lr: 0.0003